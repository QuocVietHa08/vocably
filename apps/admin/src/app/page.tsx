// Admin panel — scaffold
// TODO: build dashboard with user sessions, word analytics, vocabulary management

export default function AdminPage() {
  return (
    <main style={{ padding: 40, fontFamily: 'system-ui' }}>
      <h1 style={{ fontWeight: 700, fontSize: 24 }}>IELTS Coach — Admin</h1>
      <p style={{ color: '#666', marginTop: 8 }}>
        Dashboard coming soon. Run <code>npm run dev:admin</code> from the root.
      </p>
    </main>
  );
}
